import React from "react";
import EntityWrapper from "../component-parts/EntityWrapper";

const LocationDetail: React.FC = () => {
  return <EntityWrapper target="location" />;
};

export default LocationDetail;
