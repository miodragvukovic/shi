import React from "react";
import EntityWrapper from "../component-parts/EntityWrapper";

const EpisodeDetail: React.FC = () => {
  return <EntityWrapper target="episode" />;
};

export default EpisodeDetail;
